# Rail Dispatch benchmark — Stage 0

Work only inside this workspace. Do not inspect parent or sibling directories and do not use network access.

Complete the Stage 0 contract in README.md and improve the existing implementation. Run the public tests. This is a multi-agent architecture benchmark: use your native delegation/subtask capability when it is useful, but do not delegate trivial work solely to increase agent count. All root and child agents must use the already configured gpt-5.6-sol route; never request, select, or invoke gpt-6, gpt-6-astra, Astra, or any other model. Do not finish until code and public tests are in a coherent state.
