# Stage 3 — physical constraints
Enforce section capacity, train length limits, and minimum headway between movements. Never return an unsafe active state, even for malformed or adversarial event orderings. Reject invalid API inputs consistently.
